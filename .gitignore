# Local tooling — not part of the website
/.claude/

# Local prompt / scratch notes — not part of the portfolio
/Prompt.txt

# Standalone documentation packages — kept local-only (the website reads projects/, not this).
# Leading slash anchors to the ROOT folder so it never matches projects/artifacts/<slug>/.
/hermes/
/prometheus/

# OS / editor noise
Thumbs.db
.DS_Store
